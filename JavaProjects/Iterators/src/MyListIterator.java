public class MyListIterator implements ListIterator{

    private ListNode root;
    public MyListIterator(LinkedList list) {
        this.root = list.;
    }

    @Override
    public ListNode next() {

        Point result;
        if (!hasNext()) return null;
        result =
    }

    @Override
    public boolean hasNext() {
        return false;
    }`

}
